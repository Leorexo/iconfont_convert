name: iconfont_convert
description: A library to generate Flutter IconData Class from www.iconfont.cn
version: 1.0.1
homepage: https://github.com/wkan/iconfont_convert

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  yaml: ^3.1.1
  args: ^2.4.0
  http: ^1.1.0
  path: ^1.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

executables:
  iconfont_convert:
